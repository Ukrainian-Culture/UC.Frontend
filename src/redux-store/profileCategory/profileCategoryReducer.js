const initialState = {
    category: {
        '0' : ['profile', 'history'],
        '1' : ['профіль', 'історія']
    }
}

const profileCategoryReducer = (state = initialState, action) => {
    return state;
}

export default profileCategoryReducer;